apiVersion: v1
kind: Service
metadata:
  name: {{ include "secure-task-manager.fullname" . }}
  labels:
    {{- include "secure-task-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secure-task-manager.selectorLabels" . | nindent 4 }}
